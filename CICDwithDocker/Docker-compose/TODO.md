# TODO

- [x] Update `tsconfig.json` to compile `lib/**/*` (not only `src/**/*`).
- [ ] Update `src/index.ts` import from `../lib/prisma.js` to `../lib/prisma`.
- [ ] Fix Prisma client import so it works from `dist/` as well.
- [ ] Rebuild (`npm run build`).
- [ ] Run (`npm run dev`) and ensure endpoints work (GET / and POST /).

